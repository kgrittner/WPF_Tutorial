<Window x:Class="ProWPF_C.WPFCookbook.Ch2Resources.UserSelectedColorsFonts"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="UserSelectedColorsFonts" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Rectangle Margin="10" Fill="{DynamicResource {x:Static SystemColors.DesktopBrushKey}}" />
        <TextBlock Grid.Row="1" Text="Hello from Active Caption Font" 
                   FontFamily="{DynamicResource {x:Static SystemFonts.CaptionFontFamilyKey}}" 
                   FontSize="{DynamicResource {x:Static SystemFonts.CaptionFontSizeKey}}"/>
    </Grid>
</Window>
